#include <iostream>

using std::cout;
using std::cin;
using std::endl;

enum class Motion {
    Forward,
    Back,
    Left,
    Right,
};

int main(void)
{
    Motion m = Motion::Back;
    
    
    
    return 0;
}