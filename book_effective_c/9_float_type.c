#include <limits.h>
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(void){
    int real = 10;
    float floating = 2.3;

    double x = NAN;
    double y = INFINITY;
    // printf("%f, %f", x, y);
    // int z = fpclassify(real - floating);

    return EXIT_SUCCESS;
}